[pytest]
# ใช้ temp dir ภายใน repo แทน system temp
# กันปัญหา PermissionError บน Windows เมื่อ %TEMP%\pytest-of-<user> ถูกล็อก/สิทธิ์เสีย
# (.pytest_tmp/ ถูก ignore ใน .gitignore แล้ว)
addopts = --basetemp=.pytest_tmp
testpaths = tests
